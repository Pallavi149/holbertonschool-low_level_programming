#include"main.h"
/**
*_isupper - cheks for uppercase character
*@c: The character to print
*
*Return: Always return 0
*/

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}

