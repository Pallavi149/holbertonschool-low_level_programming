#include"main.h"
/**
*add - This function adds two integers and returns the result
*@a: First integer number to add
*@b: Second integer number
*
*Return: Returns the result
*/

int add(int a, int b)
{
	int c;

	c = (a + b);
	return (c);
}
