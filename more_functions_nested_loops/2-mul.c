#include"main.h"
/**
*mul - This function multiplies two integers and returns the result
*@a: First integer number to multiply
*@b: Second integer number
*
*Return: Returns the result
*/

int mul(int a, int b)
{
	int c;

	c = (a * b);
	return (c);
}
