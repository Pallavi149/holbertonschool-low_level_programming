#include"main.h"
/**
*_islower - cheks for lowercase character
*@c: The character to print
*
*Return: Always return 0
*/

int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}

