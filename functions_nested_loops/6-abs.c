#include "main.h"
/**
*_abs - Returns the absolute value of an integer
*
*@n: Number
*Return: returns absolute value
*/

int _abs(int n)
{
	if (n < 0)
	{
		return (-(n));
	}
	else
	{
		return (n);
	}
}
